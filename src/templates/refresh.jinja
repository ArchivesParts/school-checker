{% extends 'layout.jinja' %} 
{% block content %} 
    <ul class="list-group">
        <li class="list-group-item active">{{ (total|float/coef|float)|round(2, 'ceil') }}/20</li>
        <li class="list-group-item">{{created}} nouvelles notes</li>
        <li class="list-group-item">{{updated}} notes mises à jour</li>
    </ul>

{%endblock%}